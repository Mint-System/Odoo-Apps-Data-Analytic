Prepare Jupyter notebook:

- Start Jupyter lab and open the main.ipynb notebook
- Enter the following code:

import pandas as pd
df = pd.read_csv("input.csv")
df.head()

Execute:

- Open the main.ipynb notebook in the Odoo backend
- Click on "Run" and and upload the input.csv file
